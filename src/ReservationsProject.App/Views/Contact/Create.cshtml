@using ISUCorp.ReservationsProject.Core
@using ISUCorp.ReservationsProject.Core.Utils
@model ISUCorp.ReservationsProject.Domain.Dto.ContactInputDto

@{
    ViewBag.ActivePage = "Contact";
    ViewBag.Title = "Create Contact";
}

<article class="main-body">
    <div class="container">
        <div class="content">
            <form data-bind="submit: send" class="form-horizontal">

                <div class="row">
                    <div class="col-12 col-md-3">
                        <div class="form-group has-icon">
                            <label for="" class="sr-only">Name</label>
                            <span class="fa fa-users"></span>
                            <input data-bind="value: Name, validationElement: Name" name="Name" type="text" class="form-control" placeholder="Name" required>
                            @Html.ValidationMessageFor(m => m.Name)
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="form-group has-icon">
                            <label for="" class="sr-only">Contact type</label>
                            <span class="fa fa-globe"></span>
                            @Html.DropDownListFor(contactDto => contactDto.Type, Enum.GetValues(typeof(ContactType)).Cast<ContactType>().Select(x => new SelectListItem { Text = x.GetDescription(), Value = ((int)x).ToString() }), new { @class = "form-control", data_bind = "value: Type" })
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="form-group has-icon">
                            <label for="" class="sr-only">Phone</label>
                            <span class="fa fa-phone"></span>
                            <input data-bind="value: Phone" type="text" class="form-control" placeholder="Phone">
                        </div>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="form-group has-icon">
                            <label for="" class="sr-only">Birthday</label>
                            <span class="fa fa-calendar"></span>
                            <input data-bind="value: Birthdate" type="date" class="form-control" placeholder="Birthday" required="">
                        </div>
                    </div>
                </div>

                <div id="edit"></div>

                <div class="form-actions">
                    <a class="btn btn-sm btn-gray" href="@Url.Action("List", "Contact")">Cancel</a>
                    <input class="btn btn-sm" type="submit" value="Send">
                </div>

            </form>
        </div>
    </div>
</article>

@section scripts
{
    <script src="~/Scripts/Contact/CreateViewModel.js"></script>
}